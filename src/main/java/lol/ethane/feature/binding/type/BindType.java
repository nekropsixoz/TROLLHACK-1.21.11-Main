package lol.ethane.feature.binding.type;

public enum BindType {
   MODULE,
   CONFIG;

   // $FF: synthetic method
   private static BindType[] $values() {
      return new BindType[]{MODULE, CONFIG};
   }
}
